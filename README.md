@@ .. @@
+## 🔄 نسخ المشروع مع قاعدة بيانات جديدة
+
+إذا كنت تريد نسخ هذا المشروع واستخدام قاعدة بيانات منفصلة:
+
+### 1. إنشاء مشروع Supabase جديد
+```bash
+# اذهب إلى https://supabase.com
+# أنشئ مشروع جديد
+# احصل على URL و API Keys
+```
+
+### 2. إعداد متغيرات البيئة
+```bash
+# انسخ ملف .env.example إلى .env
+cp .env.example .env
+
+# حرر الملف وأضف بيانات قاعدة البيانات الجديدة
+nano .env
+```
+
+### 3. تشغيل Migration Scripts
+```bash
+# تأكد من أن قاعدة البيانات الجديدة تحتوي على نفس الجداول
+# يمكنك نسخ SQL من ملفات migrations/ وتشغيلها في SQL Editor
+```
+
+### 4. اختبار الاتصال
+```bash
+npm run dev
+# تأكد من أن التطبيق يتصل بقاعدة البيانات الجديدة
+```
+
+---
+
 # تطبيق الحسابات المالية